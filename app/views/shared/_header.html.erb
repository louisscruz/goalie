<nav class="navbar navbar-light bg-faded">
  <a class="navbar-brand" href="#">Goalie</a>
  <% if current_user %>
    <%= button_to "Sign Out", session_url, method: "delete", class: "btn btn-success" %>
    <%= current_user.name %>
  <% else %>

  <% end %>
  <ul class="nav navbar-nav">
    <!-- <li class="nav-item active">
      <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Features</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Pricing</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">About</a>
    </li> -->
    <% if current_user %>
      <%= link_to "Sign Out", session_url, method: "delete", class: "btn btn-default" %>
      <%= current_user.name %>
    <% else %>
      <%= link_to "Sign In", new_session_url, method: "get", class: "btn btn-default" %>
      <%= link_to "Sign Up", new_user_url, method: "get", class: "btn btn-success" %>
    <% end %>
  </ul>
</nav>
